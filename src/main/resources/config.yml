# CraftplayShop Hauptkonfiguration.
# Lizenz: Proprietär. Copyright Wullverin @ Craftplay.de.

settings:
  # Standardsprache des Plugins, wenn ein Spieler keine eigene Sprache gewählt hat.
  defaultLanguage: "de_DE"

  # Ersatzsprache, falls eine Übersetzung oder GUI-Datei fehlt.
  fallbackLanguage: "en_US"

  # Erlaubt Spielern, ihre Sprache selbst umzuschalten.
  allowPlayerLanguageSwitch: true

  # Aktiviert zusätzliche Debug-Ausgaben.
  debug: false

commands:
  # Hauptbefehl, den du für CraftplayShop verwenden möchtest.
  # In Phase 1 können die in plugin.yml registrierten Befehle genutzt werden:
  # "cshop", "shop" oder "craftplayshop".
  # Beispiel: "cshop" => /cshop admin editor
  pluginCommand: "cshop"

files:
  # Wenn true, ergänzt CraftplayShop beim Start und Reload vorhandene
  # config-, language- und gui-Dateien automatisch um fehlende neue Keys.
  # Bestehende Werte werden dabei nicht überschrieben.
  autoUpdateExisting: true

economy:
  # Wenn true, wird Vault benötigt. Fehlt Vault, deaktiviert sich das Plugin.
  requireVault: true

  # Symbol oder Name der Währung.
  currencySymbol: "Coins"

  # Format für Geldbeträge.
  format: "%amount% Coins"

database:
  # SQLITE oder MYSQL.
  type: "SQLITE"

  # Prefix für alle Datenbanktabellen.
  tablePrefix: "craftplay_shop_"

mysql:
  # MySQL/MariaDB Host.
  host: "localhost"
  port: 3306
  database: "minecraft"
  username: "root"
  password: ""
  useSSL: false

gui:
  # Aktiviert externe GUI-Dateien aus dem /gui Ordner.
  enabled: true

  # Erstellt fehlende GUI-Dateien aus Ressourcen.
  createMissingFiles: true

  # Nutzt fallbackLanguage, wenn GUI-Datei oder Key fehlt.
  useFallbackLanguage: true

  # Cache für GUI-Items.
  cacheGuiItems: true

  # Erlaubt Console Commands auf GUI-Buttons.
  allowConsoleCommands: true

  # Erlaubt Player Commands auf GUI-Buttons.
  allowPlayerCommands: true

  # Blockiert gefährliche Befehle.
  blockDangerousCommands: true

  blockedCommandPrefixes:
    - "op "
    - "deop "
    - "stop"
    - "restart"
    - "reload"

language:
  # Erstellt fehlende Sprachdateien.
  createMissingFiles: true

  # Spieler dürfen Sprache wechseln.
  playerSwitchEnabled: true

serverShop:
  enabled: true
  allowBuy: true
  allowSell: true
  defaultCurrency: "VAULT"

  # Standard-Itemvergleich für einfache ServerShop-Items.
  itemMatchMode: "MATERIAL_ONLY"

  amountSelection:
    # Wenn true, steht die Mengenauswahl für spätere GUI-Aktionen bereit.
    # Der normale Spieler-Klick im ServerShop nutzt standardmäßig das EconomyShopGUI-System:
    # Linksklick kauft 1, Shift-Linksklick kauft einen Stack, Rechtsklick verkauft 1,
    # Shift-Rechtsklick verkauft einen Stack.
    enabled: false

    # Vorgegebene Mengen für Kaufen und Verkaufen.
    amounts:
      - 1
      - 8
      - 16
      - 32
      - 64

    # Wenn true, können Spieler eine eigene Menge per Chat eingeben.
    allowCustomAmount: true

    # Maximale eigene Menge aus der Chat-Eingabe.
    maxCustomAmount: 64

  buyConfirmation:
    # Wenn true, müssen teure Käufe im ServerShop bestätigt werden.
    enabled: true

    # Ab welchem Gesamtpreis eine Bestätigungs-GUI angezeigt wird.
    threshold: 10000.0

  sellProtection:
    # Blockiert ServerShop-Verkäufe aus dem Creative-Modus.
    blockCreative: true

    # Blockiert ServerShop-Verkäufe aus dem Spectator-Modus.
    blockSpectator: true

    # Blockiert den Verkauf beschädigter Items.
    # Wichtig bei MATERIAL_ONLY-Matching, damit abgenutzte Tools nicht wie neue Items verkauft werden.
    blockDamagedItems: true

  protection:
    worlds:
      # Wenn true, wird die Weltenliste unten ausgewertet.
      enabled: false

      # BLOCKLIST blockiert nur die genannten Welten.
      # ALLOWLIST erlaubt nur die genannten Welten.
      mode: "BLOCKLIST"

      # Weltname exakt wie im Server.
      list: []

    gameModes:
      # Wenn true, dürfen nur die unten genannten GameModes kaufen/verkaufen.
      enabled: true

      buyAllowed:
        - "SURVIVAL"
        - "ADVENTURE"

      sellAllowed:
        - "SURVIVAL"
        - "ADVENTURE"

adminShop:
  backups:
    # Erstellt vor Änderungen über den Ingame-Editor ein Backup der server_shop.yml.
    enabled: true

    # Unterordner im Plugin-Ordner für ServerShop-Backups.
    folder: "backups/server_shop"

    # Mindestabstand zwischen zwei automatischen Backups.
    # Verhindert Backup-Spam bei mehreren schnellen Preis- oder Sortierklicks.
    cooldownSeconds: 30

    # Maximale Anzahl gespeicherter ServerShop-Backups.
    maxFiles: 25

    # Anzahl der Backups, die /cshop admin backup list maximal im Chat anzeigt.
    listLimit: 8

sellCommands:
  sellHand: true
  sellAll: true
  sellInventoryGui: true

playerShops:
  enabled: true
  useGuiCreation: true

  # Itemvergleich für PlayerShop-Kisten.
  # EXACT prüft Material und ItemMeta, MATERIAL_ONLY nur das Material.
  itemMatchMode: "EXACT"

  creation:
    # Standard-Display-Typ für neu erstellte Kistenshops.
    # NONE, ITEM, GLASS_CASE, LARGE_ITEM oder ITEM_FRAME.
    defaultDisplayType: ITEM

    # Spieler können mit Shift-Linksklick auf eine Kiste einen Chat-Assistenten starten.
    chatCreationEnabled: true

    # Material für automatisch gesetzte Shop-Schilder.
    autoSignMaterial: OAK_WALL_SIGN

    # Material für automatische stehende Schilder, wenn oben auf eine Kiste geklickt wird.
    autoStandingSignMaterial: OAK_SIGN

    # Spieler können einen SELL-Kistenshop erstellen, indem sie ein Schild mit [shop]
    # neben eine Kiste setzen. Zeile 2 = Menge, Zeile 3 = Preis.
    signCreationEnabled: true

    # Erlaubte Marker in der ersten Schildzeile.
    signMarkers:
      - "[shop]"
      - "[cshop]"
      - "shop"
      - "sell"
      - "buy"
      - "ankaufen"

  sign:
    # Statusfarben und Statustexte für PlayerShop-Schilder.
    # Bei SELL-Shops ist ein Shop verfügbar, wenn genug passende Items in der Kiste liegen.
    # Bei BUY-Shops ist ein Shop verfügbar, wenn genug Platz in der Kiste ist und der Besitzer genug Geld hat.
    status:
      availableColor: "&a"
      unavailableColor: "&c"
      availableText: "Verfügbar"
      unavailableText: "Nicht verfügbar"

    statusRefresh:
      # Aktualisiert Shop-Schilder automatisch, wenn sich Kisteninhalte ändern.
      # Zusätzlich werden Inventarklicks, Inventar-Schließen und Hopper-Bewegungen sofort nachgezogen.
      enabled: true

      # Wie oft Shop-Schilder im Hintergrund geprüft werden.
      intervalTicks: 20

      # Maximal aktualisierte Schilder pro Durchlauf.
      maxSignsPerTick: 25

    # Verfügbare Schild-Platzhalter:
    # %action% = Verkaufen oder Ankaufen
    # %action_en% = Selling oder Buying
    # %type% = SELL oder BUY
    # %owner% = Shopbesitzer
    # %amount% = Menge pro Handel
    # %item% = Itemname oder Material
    # %price% = formatierter Preis
    # %stock% = vorhandene passende Items in der Kiste
    # %space% = freier Platz für passende Items in der Kiste
    # %status% = verfügbarer oder nicht verfügbarer Statustext
    # %available% = true oder false
    # %status_color% und %stock_color% = configured Statusfarbe
    # Alte [owner], [amount], [item], [price], [stock color] Platzhalter funktionieren weiterhin.
    sell:
      # Schildtext für SELL-Shops nach dem Erstellen.
      lines:
        - "%status_color%&l%action%"
        - "&f%amount%x %item%"
        - "&a%price%"
        - "&8%owner%"
    buy:
      # Schildtext für BUY-Shops, die Items von Spielern ankaufen.
      lines:
        - "%status_color%&l%action%"
        - "&f%amount%x %item%"
        - "&a%price%"
        - "&8%owner%"
    combo:
      # Schildtext für BUY_SELL-Shops. Rechtsklick kauft aus dem Shop, Linksklick verkauft an den Shop.
      lines:
        - "%status_color%&l%action%"
        - "&f%amount%x %item%"
        - "&a%price%"
        - "&8%owner%"
    trade:
      # Schildtext für TRADE_ITEM-Shops. Spieler tauschen %trade_amount%x %trade_item% gegen %amount%x %item%.
      lines:
        - "%status_color%&l%action%"
        - "&f%amount%x %item%"
        - "&egegen %trade_amount%x %trade_item%"
        - "&8%owner%"
    pending:
      # Schildtext, wenn der Spieler das Item nicht in der Hand hatte.
      lines:
        - "&6&l[shop]"
        - "&eItem fehlt"
        - "&7Mit Item klicken"
        - "&8%owner%"

  display:
    # Standard-Anzeige über neu erstellten Kistenshops.
    # NONE, ITEM, GLASS_CASE, LARGE_ITEM oder ITEM_FRAME.
    # Alias für ältere Configs. Neue Shops nutzen playerShops.creation.defaultDisplayType.
    defaultType: ITEM

    animation:
      item:
        # Wenn true, schweben ITEM-Displays leicht auf und ab und drehen sich.
        enabled: true

        # Wie oft die Animation aktualisiert wird.
        intervalTicks: 1

        # Clientseitige Interpolation der Display-Transformation.
        # Höhere Werte wirken weicher, reagieren aber etwas träger.
        interpolationTicks: 4

        # Maximale Höhe der Auf-/Ab-Bewegung in Blöcken.
        hoverAmplitude: 0.08

        # Geschwindigkeit der Auf-/Ab-Bewegung.
        hoverSpeed: 1.35

        # Drehgeschwindigkeit in Grad pro Tick.
        rotationDegreesPerTick: 1.5

        # Schutzwert, damit bei sehr vielen Shops nicht alle Displays in einem Tick bewegt werden.
        maxAnimatedDisplaysPerTick: 150

  cleanup:
    # Prüft regelmäßig, ob Kiste und Schild noch existieren.
    # Fehlende physische Shops werden aus Cache und Datenbank entfernt.
    enabled: true
    intervalSeconds: 60

  protection:
    # Blockiert das Öffnen fremder PlayerShop-Kisten.
    blockContainerOpenByOthers: true

    # Löscht einen PlayerShop automatisch, wenn Besitzer oder Admin Schild/Kiste abbauen.
    deleteShopOnOwnerBreak: true

  plotsquared:
    # Wenn PlotSquared vorhanden ist, werden PlayerShops innerhalb eines gelöschten Plots
    # automatisch aus Cache und Datenbank entfernt.
    deleteShopsOnPlotDelete: true

autoSellChest:
  # Aktiviert AutoSellChest. Bestehende Configs behalten ihren Wert und können hier auf true gesetzt werden.
  enabled: true

  # Anzeigename für neu registrierte Verkaufskisten. %id% wird durch die Datenbank-ID ersetzt.
  defaultName: "&cAutoSellChest #%id%"

  # Besitzer erhalten standardmäßig eine Nachricht, wenn ihre Kiste Items verkauft.
  notifyOwnerDefault: true

  # Spezial-Item, das per /asc give <spieler> [menge] ausgegeben und platziert wird.
  item:
    material: CHEST
    name: "&cAutoSellChest"
    lore:
      - "&7Platziere diese Kiste, um eine"
      - "&7automatische Verkaufskiste zu erstellen."
      - ""
      - "&eVerkauft an den Craftplay ServerShop."
    customModelData: 0
    dropItemOnBreak: true

  maxChests:
    # Admins mit craftplayshop.autosellchest.admin umgehen das Limit.
    adminBypass: true

    # Standardlimit pro Spieler. -1 bedeutet unbegrenzt.
    default: 5

    # Spieler mit craftplayshop.autosellchest.limit.<key> nutzen den höchsten passenden Wert.
    permissionOverrides:
      vip: 10
      premium: 20
      admin: -1

  selling:
    # BOTH ist für spätere Modi vorbereitet. Aktuell verkauft AutoSellChest immer an den ServerShop.
    mode: "BOTH"

    # Mindestabstand zwischen automatischen Verkaufsdurchläufen je Kiste.
    intervalSeconds: 10

    # Wenn true, verkauft eine Kiste nur, solange der Besitzer online ist.
    sellOnlyWhenOwnerOnline: false

    # Wenn false, bleiben beschaedigte Tools, Ruestungen und andere kaputte Items in der Kiste.
    allowDamagedItems: false

    materialFilter:
      # Optionaler Materialfilter nur fuer AutoSellChest.
      enabled: false

      # BLOCKLIST sperrt die gelisteten Materialien, ALLOWLIST erlaubt nur die gelisteten Materialien.
      mode: "BLOCKLIST"

      # Materialnamen wie in Bukkit/Paper, z.B. BEDROCK, SPAWNER oder DIAMOND_SWORD.
      materials: []

  upgrades:
    # Aktiviert kaufbare AutoSellChest-Upgrades.
    enabled: true

    interval:
      # Aktiviert Intervall-Upgrades. Level 0 nutzt selling.intervalSeconds.
      enabled: true

      # Name für Level 0.
      defaultName: "&7Standard"

      levels:
        1:
          enabled: true
          name: "&aSchnell"
          intervalSeconds: 5
          price: 2500.0
          permission: ""
        2:
          enabled: true
          name: "&bTurbo"
          intervalSeconds: 3
          price: 7500.0
          permission: "craftplayshop.autosellchest.upgrade.interval.2"
        3:
          enabled: true
          name: "&6Blitz"
          intervalSeconds: 1
          price: 20000.0
          permission: "craftplayshop.autosellchest.upgrade.interval.3"

    multiplier:
      # Aktiviert Preis-Multiplikator-Upgrades.
      enabled: true

      # Name für Level 0.
      defaultName: "&7Standard"

      levels:
        1:
          enabled: true
          name: "&aBonus I"
          multiplier: 1.10
          price: 5000.0
          permission: ""
        2:
          enabled: true
          name: "&bBonus II"
          multiplier: 1.25
          price: 15000.0
          permission: "craftplayshop.autosellchest.upgrade.multiplier.2"
        3:
          enabled: true
          name: "&6Bonus III"
          multiplier: 1.50
          price: 40000.0
          permission: "craftplayshop.autosellchest.upgrade.multiplier.3"

  trust:
    # Aktiviert das AutoSellChest-Trust-System in der GUI.
    enabled: true

    # Standardrechte, wenn ein Spieler neu vertraut wird.
    defaultRights:
      open: true
      manage: false
      upgrade: false
      delete: false

  messages:
    # Sendet dem Besitzer eine kurze Nachricht, wenn online und seine Kiste verkauft hat.
    notifyOwnerOnSell: true

    # SUMMARY sendet eine Sammelmeldung pro Verkauf, PER_ITEM eine Zeile je Material.
    notifyMode: "SUMMARY"

    # Mindestwert eines Verkaufs, bevor eine Besitzer-Nachricht gesendet wird.
    minimumTotalPrice: 0.0

    # Mindestabstand zwischen zwei Besitzer-Nachrichten derselben AutoSellChest.
    cooldownSeconds: 0

  display:
    # Zeigt ein TextDisplay über jeder geladenen AutoSellChest.
    enabled: true

    # Position relativ zum AutoSellChest-Block.
    offset:
      x: 0.5
      y: 1.45
      z: 0.5

    # Aktualisierungsintervall in Ticks.
    refreshIntervalTicks: 40

    # Wenn true, ist der Text durch Blöcke sichtbar.
    seeThrough: false

    # Aktiviert Text-Schatten.
    shadow: true

    # Verfügbare Platzhalter:
    # %id%, %name%, %owner%, %status%, %world%, %x%, %y%, %z%,
    # %interval%, %next_sell%, %next_sell_seconds%, %multiplier%, %total_items%, %total_money%, %debug%,
    # %owner_uuid%, %location%, %notify_status%, %notify_processor_status%, %owner_online%
    lines:
      - "&c%name%"
      - "&7Besitzer: &f%owner%"
      - "&7Status: %status%"
      - "&7Nächster Verkauf: &f%next_sell%"
      - "&7Multiplikator: &fx%multiplier%"

  statistics:
    # Anzahl letzter Verkäufe im Statistik-GUI.
    recentLimit: 8

    # Anzahl Material-Zusammenfassungen im Statistik-GUI.
    materialLimit: 8

  effects:
    placeParticles: true
    placeSound: true

  performance:
    # Takt, in dem bekannte AutoSellChests verarbeitet werden. Es werden keine Welt-Scans gemacht.
    scanIntervalSeconds: 10

    # Maximale AutoSellChests pro Durchlauf.
    maxChestsPerTick: 5

    # Maximale Item-Anzahl, die pro Kiste in einem Durchlauf geprüft wird.
    maxItemsPerScan: 2304

    # Wenn true, werden nur Kisten verarbeitet, die durch Hopper/Inventar-Events als dirty markiert wurden.
    # Wenn false, werden zusätzlich alle fälligen Kisten anhand von intervalSeconds verarbeitet.
    processOnlyDirtyChests: false

    # Dirty-Kisten werden erst nach dieser Ruhezeit verkauft, damit Hopper-Bursts gebündelt werden.
    dirtyCooldownSeconds: 5

    # Schreibt AutoSellChest-Logs asynchron.
    asyncDatabaseLogging: true

  cleanup:
    # Prüft regelmäßig, ob registrierte AutoSellChests physisch noch existieren.
    # Es werden nur bereits bekannte und geladene Kisten geprüft, keine Welt-Scans.
    enabled: true
    intervalSeconds: 60

  protection:
    # Blockiert normales Öffnen fremder AutoSellChests.
    protectContainer: true

    # Nur Besitzer oder Admins dürfen AutoSellChests abbauen.
    blockBreakingByOthers: true

    # Explosionen zerstören AutoSellChests nicht.
    blockExplosionDamage: true

    # Hopper dürfen keine Items aus AutoSellChests ziehen.
    blockHopperExtraction: true

    # Hopper dürfen Items in AutoSellChests einfügen.
    allowHopperInsertion: true

  plotsquared:
    # Wenn PlotSquared vorhanden ist, werden AutoSellChests innerhalb eines gelöschten Plots
    # automatisch aus Cache und Datenbank entfernt.
    deleteChestsOnPlotDelete: true

directTrade:
  enabled: true

  # Spieler können DirectTrade für sich selbst deaktivieren.
  allowPlayerToggle: true

  # Standardstatus für neue Spieler.
  defaultEnabledForPlayers: true

  # Wenn true, darf ein Spieler keine Anfragen erhalten, wenn er DirectTrade deaktiviert hat.
  blockRequestsWhenDisabled: true

  # Wenn true, darf ein Spieler keine Anfragen senden, wenn er DirectTrade deaktiviert hat.
  blockSendingWhenDisabled: true

  request:
    timeoutSeconds: 30
    cooldownSeconds: 10
    requireSameWorld: true
    maxDistance: 10.0

  confirmation:
    requireReady: true
    requireFinalConfirm: true
    resetReadyOnChange: true

  money:
    enabled: true
    minAmount: 1.0
    maxAmount: 1000000.0

rankShop:
  enabled: true

  execution:
    addGroupCommandFormat: "lp user %player% parent add %group%"
    temporaryAddGroupCommandFormat: "lp user %player% parent addtemp %group% %duration%"
    removeGroupCommandFormat: "lp user %player% parent remove %group%"

  products:
    vip:
      enabled: true
      displayName: "&aVIP Rang"
      material: NAME_TAG
      slot: 11
      price: 25000.0
      group: "vip"
      duration: ""
      oneTime: true
      requiredPermissions: []
      ownedPermissions:
        - "group.vip"
      removeGroups: []
      commands: []
      playerCommands: []
      lore:
        - "&7Schalte den VIP Rang frei."
        - ""
        - "&7Preis: &e%price%"
        - "&eKlicken zum Kaufen"
    vip_temp:
      enabled: true
      displayName: "&bVIP 30 Tage"
      material: CLOCK
      slot: 13
      price: 12000.0
      group: "vip"
      duration: "30d"
      oneTime: false
      requiredPermissions: []
      ownedPermissions: []
      removeGroups: []
      commands: []
      playerCommands: []
      lore:
        - "&7Temporärer VIP Rang."
        - ""
        - "&7Dauer: &e%duration%"
        - "&7Preis: &e%price%"
        - "&eKlicken zum Kaufen"
    premium_upgrade:
      enabled: true
      displayName: "&6Premium Upgrade"
      material: DIAMOND
      slot: 15
      price: 50000.0
      group: "premium"
      duration: ""
      oneTime: true
      requiredPermissions:
        - "group.vip"
      ownedPermissions:
        - "group.premium"
      removeGroups:
        - "vip"
      commands: []
      playerCommands: []
      lore:
        - "&7Upgrade von VIP auf Premium."
        - ""
        - "&7Preis: &e%price%"
        - "&eKlicken zum Kaufen"

permissionShop:
  enabled: true

  execution:
    permissionCommandFormat: "lp user %player% permission set %permission% true"
    temporaryPermissionCommandFormat: "lp user %player% permission settemp %permission% true %duration%"

  products:
    fly:
      enabled: true
      displayName: "&bFly"
      material: FEATHER
      slot: 11
      price: 15000.0
      duration: ""
      oneTime: true
      requiredPermissions: []
      ownedPermissions:
        - "cmi.command.fly"
      permissions:
        - "cmi.command.fly"
        - "cmi.command.fly.self"
      commands: []
      playerCommands: []
      lore:
        - "&7Schaltet Fly frei."
        - ""
        - "&7Preis: &e%price%"
        - "&eKlicken zum Kaufen"
    fly_temp:
      enabled: true
      displayName: "&eFly 1 Stunde"
      material: CLOCK
      slot: 13
      price: 2500.0
      duration: "1h"
      oneTime: false
      requiredPermissions: []
      ownedPermissions: []
      permissions:
        - "cmi.command.fly"
        - "cmi.command.fly.self"
      commands: []
      playerCommands: []
      lore:
        - "&7Temporäres Fly."
        - ""
        - "&7Dauer: &e%duration%"
        - "&7Preis: &e%price%"
        - "&eKlicken zum Kaufen"
    kitbundle:
      enabled: true
      displayName: "&6Starter Bundle"
      material: CHEST
      slot: 15
      price: 5000.0
      duration: ""
      oneTime: false
      requiredPermissions: []
      ownedPermissions: []
      permissions: []
      commands:
        - "crate key give %player% starter 1"
      playerCommands: []
      lore:
        - "&7Fuehrt ein Paket aus Commands aus."
        - ""
        - "&7Preis: &e%price%"
        - "&eKlicken zum Kaufen"

referral:
  enabled: false

auctionHouse:
  enabled: true

  fees:
    # Feste Gebuehr pro Listing.
    flat: 0.0

    # Prozentuale Gebuehr pro Listing. 0.05 = 5 Prozent.
    percentage: 0.05

  defaults:
    # Standardpreis fuer GUI-Listing-Aktionen mit der Hand.
    price: 100.0

    # Standardlaufzeit eines Listings.
    durationHours: 48

integrations:
  placeholderApi:
    enabled: true

  headDatabase:
    enabled: true
    fallbackMaterial: PLAYER_HEAD
    warnIfMissing: true

  floodgate:
    enabled: true

  discord:
    enabled: false

protection:
  worldguard: true
  plotsquared: true
  lands: true
  bentobox: true

performance:
  asyncDatabaseLogging: true
  guiCacheEnabled: true
  maxGuiBuildsPerTick: 10

panel:
  # Eindeutige ID dieses Minecraft-Servers für das externe Team Panel.
  # Im Panel muss dieselbe ID bei der CraftplayShop-Server-Konfiguration eingetragen werden.
  # Beispiel: "Testserver-1"
  serverId: "change-this-server-id"

  # Geheimer Token für spätere Panel-API-Zugriffe auf dieses Plugin.
  # Trage denselben Token im Panel bei diesem Server ein. IP und Port werden nur im Panel gepflegt.
  token: "change-this-token"
